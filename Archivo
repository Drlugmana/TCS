// src/utils/config.js
const BASE = import.meta.env.VITE_API_URL || process.env.REACT_APP_API_URL;

// Devuelve la base para Problems
export function getApiBaseUrl() {
  // https://... + /api/Problems
  return `${BASE}/api/Problems`;
}
