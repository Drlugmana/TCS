
[HttpGet("health")]
public IActionResult Health([FromServices] IConfiguration cfg)
{
    var rel = cfg["JurisdictionModel:Path"] ?? "ML/Models/jurisdiction_model.zip";
    var full = Path.GetFullPath(rel, AppContext.BaseDirectory);

    var results = new List<string>();

    // 1) Verificación del modelo en disco
    if (System.IO.File.Exists(full))
        results.Add($"Model: OK ✅ (found at {full})");
    else
        results.Add($"Model: MISSING ❌ (expected at {full})");

    // 2) Verificación de inferencia del modelo ML
    try
    {
        var engine = MLModelInitializer.GetEngine();
        var pred = engine.Predict(new JurisdictionInput { Text = "test example" });
        results.Add($"Inference: OK ✅ (PredictedLabel = {pred.PredictedLabel})");
    }
    catch (Exception ex)
    {
        results.Add($"Inference: FAIL ❌ ({ex.GetType().Name}: {ex.Message})");
    }

    // 3) (Opcional) Comprobación de conectividad externa
    try
    {
        using var http = new HttpClient { Timeout = TimeSpan.FromSeconds(5) };
        var resp = http.GetAsync("https://www.google.com").Result;
        results.Add(resp.IsSuccessStatusCode ? "Internet: OK 🌐" : "Internet: FAIL ❌");
    }
    catch (Exception ex)
    {
        results.Add($"Internet: FAIL ❌ ({ex.GetType().Name})");
    }

    return Ok(new { results });
}
