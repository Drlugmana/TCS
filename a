// 🔥 TODOS los OPEN se refrescan
var openProblemIds = await FetchOpenProblemIdsAsync(token);

var openProblems = await FetchProblemByIdsAsync(openProblemIds, token);

// 👉 esto SIEMPRE actualiza comentarios, estado, etc.
await _problemRepository.InsertProblemsBatchAsync(openProblems);